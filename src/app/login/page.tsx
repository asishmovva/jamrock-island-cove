export default function LoginPage() {
  return (
    <section className="container py-12">
      <div className="space-y-3">
        <p className="text-sm font-semibold text-muted-foreground">Account</p>
        <h1 className="text-3xl font-bold">Login placeholder</h1>
        <p className="max-w-2xl text-muted-foreground">
          Authentication will be wired with Supabase Auth in a later PR. This placeholder confirms
          routing and layout only.
        </p>
      </div>
    </section>
  );
}
